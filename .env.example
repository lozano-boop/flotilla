# Ejemplo de variables de entorno (NO subir secretos al repo)
# Copia este archivo a `.env` o utiliza variables de entorno en tu CI/CD

# Local Postgres (desarrollo)
DATABASE_URL=postgres://flotilla:flotilla@localhost:5432/flotilla

# Neon (producción/servicio serverless) - reemplaza con tu URI
# Ejemplo genérico (NO usar valores reales aquí):
# NEON_DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<db>?sslmode=require

# Otros valores usados por la app (ejemplos)
# USER_RFC=XAXX010101000
# NODE_ENV=development